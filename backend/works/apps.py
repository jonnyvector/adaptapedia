"""App configuration for works."""
from django.apps import AppConfig


class WorksConfig(AppConfig):
    """Configuration for the works app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'works'
