"""App configuration for screen."""
from django.apps import AppConfig


class ScreenConfig(AppConfig):
    """Configuration for the screen app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'screen'
