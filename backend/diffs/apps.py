"""App configuration for diffs."""
from django.apps import AppConfig


class DiffsConfig(AppConfig):
    """Configuration for the diffs app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'diffs'
